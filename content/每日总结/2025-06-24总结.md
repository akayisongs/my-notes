- ##### 今天早饭吃了一个鸡蛋一杯牛奶，今天Python做了第一章的练习题，巩固了变量标识符，输入输出函数相关知识，又学了数字类型和字符串![[Pasted image 20250625001447.png]]
- ##### 今天的两道编程题分别涉及二叉树的自底向上层序遍历和寻找数组中的 K 近邻下标。首先，二叉树的自底向上层序遍历问题要求我们从叶子节点所在层到根节点所在层，逐层从左到右遍历二叉树。为了解决这个问题，我采用了广度优先搜索（BFS）策略，利用队列辅助实现层序遍历。具体步骤是先将根节点加入队列，然后进入循环：每次从队列中取出一个节点，访问该节点的值，并将其左右子节点依次加入队列。在遍历过程中，通过记录当前层的节点数量来区分不同的层，并将每层的节点值存储到一个临时数组中。遍历完成后，将结果数组反转，从而得到自底向上的层序遍历结果。这道题的关键在于如何准确地划分层次以及如何反转结果数组。另一道题是寻找数组中的 K 近邻下标，要求找出数组中所有满足至少存在一个下标 j 使得 |i - j| <= k 且 nums[j] == key 的下标 i，并按递增顺序返回。解题思路是先找出数组中所有等于 key 的下标，这可以通过一次遍历数组来实现，并将这些下标存储在一个单独的数组中。接着，使用一个布尔数组来标记每个等于 key 的下标周围 k 范围内的下标为 K 近邻下标。在标记过程中，需要注意处理数组的边界情况，确保不会越界。最后，再次遍历布尔数组，收集所有标记为 true 的下标并按顺序返回。这道题的重点在于如何高效地标记符合条件的下标，并正确处理边界条件。
- ##### 今天的剪辑做了第一个视频的四分之一，把整个视频的字幕完成了
- ![[Pasted image 20250624213210.png]]
- 