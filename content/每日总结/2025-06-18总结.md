- ##### 今天早饭吃了一个鸡蛋喝了一杯牛奶，今天依旧是复习的一天，明天就要考高数了，
- ##### 今日的两道编程题，第一题在解决二维网格单词搜索问题时，我采用了直观的**逐个击破策略**，其核心思路围绕三个关键层次展开。**首先**，我确立了以单词为中心的搜索框架：对单词列表中的每个单词独立处理，这样避免了状态混淆且逻辑清晰。**接着**，针对每个单词的搜索，我设计了双层定位机制——外层循环遍历网格的每个单元格，内层匹配单词首字母，确保所有可能的起点都被覆盖。当首字母匹配成功时，立即启动深度优先搜索（DFS）流程。**特别值得注意的是**，每次单词搜索结束后都会重置visited数组，确保不同单词的搜索互不干扰。最终，收集到的结果通过快速排序按字典序排列输出。第二道罗马数字转换题则展示了**规则抽象**的精妙解法：先建立字符到数值的ASCII映射表，再通过单次字符串遍历配合相邻字符比较统一处理转换规则，当发现当前字符值小于右侧字符值时执行减法（如IV=4），否则执行加法（如VI=6），末尾字符直接相加。这种方案将六种特殊情况自然地融入统一处理流程，无需额外判断特殊组合，其关键在于洞察到减法规则的本质就是左小右大的相邻关系；
- ##### 今天的剪辑已经导出成品了，ppt仍在完善中![[Pasted image 20250619003024.png]]