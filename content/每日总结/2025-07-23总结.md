- ##### 今天早饭吃了两个鸡蛋一块面包一根玉米和一杯牛奶，最近视频剪辑是做的关于一个木屋建造的素材的视频，我发现发布他们之后流量和点赞量有所不一样，我仔细对比了区别，发现视频长度更长的视频，解说的略多但不充满的视频点赞量浏览量都会更大，所以我逐渐往这方面靠拢了，今天的Python学到用pandas模块读写常见文件格式，web前端开发学到web语义化
- ![[Pasted image 20250724094148.png]]
- ![[Pasted image 20250724094307.png]]
- ##### 今天的两道编程题，第一道变位词映射问题要求建立两个变位词数组间的元素位置映射，核心在于使用链地址法哈希表高效处理重复元素映射。通过精心设计的哈希函数`(num % size + size) % size`确保索引非负，并用链表存储同值元素的多个位置，实现了O(n)时间复杂度的解决方案。关键挑战在于三级内存管理（结果数组/哈希表/链表节点）和严格的内存分配失败回滚机制。第二道翻转游戏问题需要生成所有有效的连续"++"翻转状态，重点在于字符串操作和内存管理。解决方案采用两阶段处理：先遍历统计连续"++"数量确定内存需求，再精确生成每个翻转状态。通过复制原字符串后局部修改字符（"++"→"--"），避免直接修改原始数据，同时实现了双层内存分配（字符串指针数组/各状态字符串）和分配失败时的级联释放；
- ##### 今天继续在上个视频的基础上剪辑了续集，逐渐在向人家那种视频节奏去做这种视频了
- 