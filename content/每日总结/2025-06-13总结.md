- ##### 今天早饭吃了一个鸡蛋喝了一碗豆浆，今天的两道编程题，第一题会议室安排最小数量本质是时间区间重叠的峰值计算，其核心在于分离会议的开始以及结束时间到独立数组并进行双排序预处理，再通过双指针扫描算法，开始指针触发会议室占用（计数器加1）、结束指针触发释放（计数器减1），同时动态捕捉计数器的全局最大值，其中边界处理的关键是当开始时间等于结束时间时优先处理结束事件以优化资源复用；第二题学生最高五科平均分，通过二级排序策略（先ID升序实现分组，再分数降序确保组内高分在前）构建有序数据结构，随后设计遍历状态机（跟踪当前学生ID、分数累加器和科目计数器）实现滑动计算窗口，仅累加前五个分数并在检测到ID切换时用整数除法即时结算平均分。两道题的共性在于都采用排序预处理（O(n log n)）将无序输入转化为有序结构，再通过单次遍历（O(n)）完成核心计算，并利用事件触发机制处理状态变化；
- ##### 今天的剪辑的视频做了一半了，应该很快能完结
- ![[Pasted image 20250614003002.png]]